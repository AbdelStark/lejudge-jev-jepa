# Decision log

Format: date · decision · options considered · reason · RFC. Append only.

| Date | Decision | Options | Reason | RFC |
|---|---|---|---|---|
| 2026-09-22 | MVP scope is PushT only | PushT; PushT+TwoRoom | One environment fully instrumented beats two half-done; adapters keyed by env keep the door open | SPEC |
| 2026-09-22 | Vocabulary default 3×3 grid + edge flags | 3×3; 4×4 | Smaller state; edge flags recover the boundary precision the edge constraints need; 4×4 kept for ablation | 0001 |
| 2026-09-22 | Soft constraints use Score | Noul; Score | Graded penalty without a threshold; confidence available | 0003 |
| 2026-09-22 | Judge the top-K elites per CEM iteration | all N; final elites only | A already ranks; K = 16 keeps questions ≤ 512; final_only ablated | 0004 |
| 2026-09-22 | Language goals are stretch, not MVP | in MVP; stretch | Demo works with goal images; retrieval adds a second judge path to validate | 0005 |
| 2026-09-22 | stable-worldmodel pinned to git commit `4821c8e6a3f0f83b7e6a80da3a757e026ea9026b` (0.1.1), not PyPI 0.0.6 | PyPI 0.0.6; git main | The LeWM checkpoint and the `planning.ShootingCostEvaluator` / `Objective` seam only exist on main; PyPI lacks `wm.lewm` | skill |
| 2026-09-22 | swm API snapshot: `World(env, num_envs, image_shape, max_episode_steps)`, `WorldModelPolicy(solver, config, process, transform)`, `PlanConfig(horizon, receding_horizon, history_len, action_block, warm_start)`, `planning.CEMSolver(cost=…, num_samples, n_steps, topk, device, seed, callbacks)`, `ShootingCostEvaluator(model, objective)`, objectives are callables `info_dict → (B, S)` reading `predicted_emb (B, S, H_ctx+horizon, 192)`; no `AutoCostModel` on main | — | Recorded at M0 as required by AGENTS.md | skill |
| 2026-09-22 | LeWM loaded from HF `quentinll/lewm-pusht` revision `22b330c2…` via `swm.wm.utils.load_pretrained` with `transformers<5` | pickled `_object.ckpt`; HF weights | The pickle references `transformers` 4.x class paths and `stable_worldmodel.wm.lewm`; the HF weights + config rebuild cleanly | 0008 |
| 2026-09-22 | JevCost is an swm `Objective` wrapping `GoalMSE` inside `ShootingCostEvaluator`; CEM iteration index via a solver callback | subclass the world model; wrap the solver | One-file adapter; `lam=0` is bit-identical to `GoalMSE` (regression test) | 0004 |
| 2026-09-22 | Judge the last 3 of 30 CEM iterations (`mode=last_n`) as the default budget | per_iter (30 calls/replan); final_only | Matches the SPEC budget of 3 Jev calls per planning step while keeping the authors' 30-iteration CEM | 0004 |
| 2026-09-22 | Expert data: official `pusht_expert_train.h5.zst` (13 GB) is read as an 8 GB streamed prefix (432k readable frames, 3.6k episodes); 800 episodes encoded for probes; action scaler fitted on all readable expert actions | full download (no disk); own weak-policy collection | Own weak-policy data gave probes R² 0.94 but a planner that exploits the model (M0 success 6 %): expert actions have std 0.20 vs 0.50, and the model was trained on expert motion. With the expert scaler and expert starts M0 success is 90 % (history 1) / 86 % (history 3) on 50 episodes | M0 |
| 2026-09-22 | M0 gate: LeWM PushT success 86 % (history 3, the paper's setting; 50 episodes, goal +25, budget 50, CEM 300×30). The paper gives no single PushT number in the text we could retrieve (Fig. 6 bars; "+18 % over PLDM"), so the gate is recorded against the authors' protocol rather than a quoted value | — | Honest reproduction record; both history settings reported | M0 |
| 2026-09-22 | Contact ground truth is geometric: agent disc within 3 px of the T polygon at the frame | simulator per-step contact counter | The expert dataset has no contact column; the simulator counter flags contact that happened *during* the step (precision 0.94 vs geometry at 3 px); a frame-level definition is what a probe can see | 0001 |
| 2026-09-22 | Speed words calibrated on expert data: still < 2 px, slow < 15 px, fast otherwise per 5-step planning block; executed-state oracles run at planner cadence | RFC draft 25 px; env-step cadence | With 25 px `fast` never occurred (gentle base rate 0); at env-step cadence displacements are 5× smaller than the judged words | 0001/0002 |
| 2026-09-22 | Study constraint sets: spatial=[centre_avoid], spatial+temporal=[centre_avoid, no_contact_first3], implicit=[gentle, approach_below]; RFC sets `edges`/`edges+upright` kept in the library but not in the main table | RFC sets | Under the expert-start protocol `edges_never` is at stake in 9 of 1,493 windows and unconstrained LeWM violates `upright_always` in 92 % of episodes (unsatisfiable within the budget); chosen sets have M0 base violation rates 30–76 % | 0002/0006 |
| 2026-09-22 | Jev requested as `jev-latest`, pinned by asserting the response model starts with `jev-1.13` (`jev-1.13.0` observed); cache key uses `jev-1.13` | request `jev-1.13` | The API rejects `jev-1.13` as a request id | 0008 |
| 2026-09-22 | LLM-small baseline = local Qwen2.5-7B-Instruct via Ollama (OpenAI-compatible endpoint), on subsets | gpt-4o-mini | The hosted OpenAI account had no credits at build time; an open model is also the PRD's stated secondary condition | 0003 |
| 2026-09-22 | Constraint library freeze: `pusht-lib@1` sha256 `61d416fdee6c37ea…` (12 constraints, 5 paraphrases, 2 negatives each, plus study sets) | — | Frozen at the start of the judge-only study | 0002 |
| 2026-09-22 | PREREG hash (sha256, first 16): `b28a9c7269606b1a` | — | Recorded before the planning study | 0006 |
| 2026-09-22 | Near-miss negative FPR is measured on items whose canonical condition is present; undefined for the oracle judge (it ignores wording) | all items | A near-miss sentence is a trap only when the canonical condition holds | 0006 |
| 2026-09-22 | Soft-family labels: episode label uses `max_fraction_fast = 0.34` while the graded score thresholds at 0.5, so even the oracle scores 0.77 accuracy on `gentle`; kept (library frozen) and reported | align thresholds | Frozen library; the mismatch is a property of soft constraints worth showing | 0002 |
| 2026-09-22 | Post-hoc (exploratory, not pre-registered) stratification of planning results by `satisfiable`: start and goal states allow meeting the constraint set (e.g. neither start nor goal block in the centre cell) | none | In the `spatial` set 42 of 90 episodes have the goal block in the centre cell and 6 more start there, so in 48 of 90 (53 %) success and compliance are mutually exclusive by construction; unconstrained LeWM violates 14 % of the 42 satisfiable episodes vs 100 % of the conflicting ones | 0006 |
| 2026-09-22 | Ablation grid extended with K = 32 and (K = 32, λ = 4) before it ran | — | The CEM refits on its top-30 elites; with K = 16 judged candidates, unjudged candidates ranked 17–30 keep their cost, which bounds the achievable shift | 0004 |
| 2026-09-22 | **Post-freeze amendment (logged, not silent):** JevCost penalises every candidate, not a K-shortlist. Hard-family questions depend only on one step's words and index, so the population's steps are de-duplicated into unique (t, facts) keys, judged once and memoised (in-process + persistent `stepfacts` table shared by all runs); soft constraints keep the K shortlist with a mean prior for unjudged candidates. Default schedule `every_k` (iterations 0, 5, …, 25 and 29) instead of `last_n` | keep RFC shortlist; per_iter | With the oracle judge (a perfect judge on probe words) the shortlist design left violation rates unchanged: in conflicting episodes every low-cost candidate violates and the CEM refits on unjudged ones; judging only the last 3 of 30 iterations cannot redirect a converged sampler. Discovered before any Jev result on the study sets; the K=16/last_n Jev/oracle/keyword partial runs are archived as `planning_*_lastn_partial.parquet` | 0004/0006 |
| 2026-09-22 | Observed with the oracle judge (per_iter, all candidates): in most violating episodes every one of the 300 initial candidates already violates (block starting in the centre cell; agent starting in contact) and where compliant candidates exist λ = 1 loses to the goal pull; the mechanism works when a compliant path exists. Reported via the `satisfiable` stratification and the λ sweep | — | Honest framing of effect sizes | 0006 |
| 2026-09-22 | Results record (MVP build). M0: 86 % (history 3) / 90 % (history 1), n = 50. Probes (linear@1, expert frames): R² block 0.992, agent 0.977, angle 0.953; bucket accuracy block 0.95, angle 0.94, edge 0.99, contact 0.95; imagined h = 5 block cell 0.88. Judge-only (256 items, probe words): Jev accuracy 0.86–0.88 canonical, 0.85–0.87 paraphrases, AUROC 0.92–0.94, ECE 0.08–0.11, near-miss FPR 0.38–0.53, std over 3 repeats 0.013; keyword 0.88–0.89 canonical, 0.64–0.74 paraphrases, FPR 0.02; Jev latency 2.5 s per 1,000 judgments. Planning (λ = 1, every_k, 90 episodes per cell): success 0.84 for every condition; violation spatial 0.56/0.56/0.56/0.57 (jev/oracle/keyword/lewm), spatial+temporal 0.84/0.83/0.81/0.86, implicit 0.59/0.52/0.56/0.57; no paired test significant after Holm; Jev gate held 49 % of candidates on spatial. Ablations on spatial (20 episodes): every setting including λ = 4, K = 64, per_iter and hard rejection leaves violation at 0.50–0.55 and success 0.95–1.00 | — | Every number traces to `artifacts/results/*.parquet`; the null planning effect is a property of the constraint/goal conflict and the world model's imagination error, established with the oracle judge | 0006 |
| 2026-09-22 | LLM-small baseline scoped down to a judge-only subset: 16 executed items, canonical texts, 12 constraints (Qwen2.5-7B-Instruct via Ollama, 64 questions per call). Result: schema-invalid answers in 67 % of calls; on valid rows accuracy 0.73, AUROC 0.74, 566 ms per judgment (≈ 230× Jev). The planning subset was not run | full subset (≈ 2 h at 35 s per call); skip | The local model is too slow for the study budget and mostly fails the 64-key JSON schema; the subset is enough to place it on the latency/accuracy axes honestly | 0006 |
| 2026-09-22 | Study 2 pre-registered (relevance-filtered starts: satisfiable from start/goal and in tension with the expert trajectory; dataset-only filter, stride 1, drawn without replacement). PREREG hash after the addendum: `9584ee87d31f7a1d` | outcome-based selection on the unconstrained plan | A planner-independent filter keeps conditions paired and the selection pre-registrable | 0006 |
| 2026-09-22 | Diagnostics on filtered spatial starts with the oracle judge: the chosen plan is imagined to violate in 20–40 % of episodes while execution violates in 65–80 %; executing the lowest-cost sampled candidate instead of the CEM mean (`PlanSpec.select="best"`, kept as an option) changes little (λ = 4 per-iteration: 0.75 → 0.65); replanning every block (receding horizon 1) collapses unconstrained LeWM success to 30 %, so it is not a usable fix. Study 2 keeps the frozen planner | shorter receding horizon; best-sample selection | The dominant residual is the world model's open-loop drift, a property of LeWM at this budget | 0004/0006 |
| 2026-09-22 | Study 2 results record (filtered starts, 90 episodes per cell). Violation LeWM / oracle / keyword / Jev: spatial 0.87 / 0.92 / 0.92 / 0.88; spatial+temporal 0.61 / 0.51★ / 0.47★ / 0.57; implicit 0.77 / 0.69 / 0.66★ / 0.73 (★ Holm-corrected paired Wilcoxon vs LeWM, p < 0.05). Success 0.76 / 0.74 / 0.77 / 0.79. Jev's gate held 62 % / 23 % / 21 % of candidates. Filtered λ sweep (Jev, spatial): 0.80–0.90 for λ ∈ {0.25, 0.5, 1, 2, 4}. H1′ (oracle −20 pts) not met; H2′ (Jev within 10 pts of oracle) met on all sets; H3′ (success within 15 pts) met | — | The confidence gate, not judge accuracy, limits Jev; τ is left at its pre-registered value and flagged as the next experiment | 0006 |
| 2026-09-22 | Study 3 pre-registered (confidence gate τ ∈ {0.75, 1.0} for Jev on the two responsive sets, filtered starts). PREREG hash after the addendum: `ae69c0a438e6ece7` | re-tune τ silently | The gate was identified as the binding constraint in Study 2; changing it must be a logged, pre-registered experiment | 0006 |
| 2026-09-22 | Study 3 results record (Jev, filtered starts, 90 episodes per cell). Violation spatial+temporal / implicit: τ = 0.5 0.57 / 0.73; τ = 0.75 0.57 / 0.77; τ = 1.0 (gate off) 0.56 / 0.69 vs LeWM 0.61 / 0.77 and oracle 0.51 / 0.69. Gate off vs LeWM: implicit −7.8 pts (Holm p = 0.039), spatial+temporal −5.6 pts (n.s.). Gate off vs τ = 0.5: −4.4 / −1.1 pts (n.s.). Success with the gate off 0.63 / 0.72 (vs 0.72 / 0.73 gated). H1″ met on both sets (gap to oracle 0.0 pt on implicit, 4.5 pts on spatial+temporal; an earlier note here mis-stated the gap as 5.6, which is the gap to LeWM); H2″ met | — | The gate was not the whole story: disabling it moves Jev to the oracle on `implicit` and within 6 points on `spatial+temporal`, but the change over the gated variant is not significant at n = 90 | 0006 |

